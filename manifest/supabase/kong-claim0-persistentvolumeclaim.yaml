apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: kong-claim0
  name: kong-claim0
  namespace: currently-working-on-db
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: manual
  volumeName: kong-pv0
status: {}
